# zerotrack

MVP clone of Glympse. Inspired by ZeroBin.

## Usage
```
$ bundle install

# start websocket server
$ ruby 0track.rb

# serve static content
$ python -mSimpleHTTPServer
```

Open `http://localhost:8000/#mysupersecretkey` in your browser and click 'send'.
